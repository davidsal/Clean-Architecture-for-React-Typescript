/**
 * Represents the response format for climate-related data.
 */
export type ClimateResponse = {
    temperature?: number; // Temperature value (optional)
    presion?: number; // Pressure value (optional)
    altitud?: number; // Altitude value (optional)
};
