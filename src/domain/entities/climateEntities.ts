/**
 * Represents the app format for climate data.
 */
 export type Climate = {
    temperature: number; // Temperature value
    pressure: number; // Pressure value
    altitude: number; // Altitude value 
};
