/**
 * Enum representing the status of an operation.
 */
export enum Status {
    OK = 'Ok', // Indicates a successful operation
    ERROR = 'Error', // Indicates an error or unsuccessful operation
    LOADING = 'Loading '// Indicates an operation is ongoing
}
